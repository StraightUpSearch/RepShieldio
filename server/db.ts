// Database configuration with strict environment separation
// Import centralized database configuration
import { db } from './config/database';

// Re-export configured database instance
export { db };

# Render Production Environment Variables
# Copy these settings to your Render dashboard Environment section

# Application Settings (REQUIRED)
NODE_ENV=production
SESSION_SECRET=your-super-secure-random-32-character-session-key-here-make-it-long

# Database Configuration (OPTIONAL)
# Leave DATABASE_URL unset to use SQLite fallback
# Set DATABASE_URL only if you have a PostgreSQL database service
# DATABASE_URL=postgresql://username:password@hostname:port/database

# API Keys (Set these based on your needs)
SCRAPINGBEE_API_KEY=your_scrapingbee_api_key_here
OPENAI_API_KEY=your_openai_api_key_here
SENDGRID_API_KEY=your_sendgrid_api_key_here

# OAuth (if using Google authentication)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Stripe (if using payment processing)
STRIPE_SECRET_KEY=your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key_here

# Notes for Render Deployment:
# 1. SESSION_SECRET is REQUIRED and must be 32+ characters
# 2. DATABASE_URL is OPTIONAL - app will use SQLite if not provided
# 3. SQLite data will not persist across deployments on Render
# 4. For production with persistent data, add a PostgreSQL service and set DATABASE_URL
# 5. All other environment variables are optional based on features used 